<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer List</title>
    <style>
        body {
            background-color: #f5f5f5; /* Light gray background */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333; /* Dark gray text color */
            margin: 0;
            padding: 20px; /* Add padding to the body */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h3 {
            color: #2ecc71; /* Emerald green text color */
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 12px;
            border: 1px solid #ddd;
            text-align: left;
        }

        th {
            background-color: #2ecc71; /* Emerald green background color for header */
            color: #fff; /* White text color for header */
        }

        tr:hover {
            background-color: #f5f5f5; /* Light gray background on hover */
        }

        .actions {
            display: flex;
            justify-content: space-between;
        }

        .edit, .delete {
            padding: 8px;
            text-decoration: none;
            color: #fff; /* White text color */
            border-radius: 5px;
            cursor: pointer;
        }

        .edit {
            background-color: #3498db; /* Blue background color for edit */
        }

        .delete {
            background-color: #e74c3c; /* Red background color for delete */
        }
    </style>
</head>
<body>
    <h3>Customer List</h3>

    <table>
        <thead>
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Street</th>
                <th>Address</th>
                <th>City</th>
                <th>State</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <!-- Sample data, replace with dynamic data from your backend -->
            
            <% for (var i =1; i <details.length; i++ ) { %>
            <tr>
                <td><%= details[i].first_name %></td>
                <td><%= details[i].last_name %></td>
                <td><%= details[i].street %></td>
                <td><%= details[i].address %></td>
                <td><%= details[i].city %></td>
                <td><%= details[i].state %></td>
                <td><%= details[i].email %></td>
                <td><%= details[i].phone %></td>

               
                <td class="actions">
                    <a href="#" class="edit">Edit</a>
                    <a href="#" class="delete">Delete</a>
                </td>
                
            </tr>
            <%# will output the numbers 1-10 %> <% } %>

           
            <!-- Add more rows for additional customers -->
        </tbody>
    </table>
</body>
</html>
